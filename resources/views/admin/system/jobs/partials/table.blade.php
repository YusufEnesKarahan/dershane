<div class="bg-white p-6 rounded-2xl shadow-premium-sm"><table class="w-full text-sm"><thead><tr class="text-left text-neutral-500"><th>Job</th><th>Durum</th><th>Başlangıç</th><th>Bitiş</th><th>Hata</th></tr></thead><tbody>@forelse($jobs as $job)<tr class="border-t"><td class="py-3">{{ class_basename($job->job_name) }}</td><td>{{ $job->status }}</td><td>{{ optional($job->started_at)->format('d.m.Y H:i') }}</td><td>{{ optional($job->completed_at)->format('d.m.Y H:i') }}</td><td class="text-red-600">{{ \Illuminate\Support\Str::limit($job->error_message,60) }}</td></tr>@empty<tr><td colspan="5" class="py-6 text-center text-neutral-500">Kayıt bulunamadı.</td></tr>@endforelse</tbody></table>{{ method_exists($jobs,'links') ? $jobs->links() : '' }}</div>
