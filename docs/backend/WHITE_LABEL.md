# White Label Management

The application features full White Labeling. Settings under the `brand` namespace (e.g. `brand.company_name`, `brand.logo`) are retrieved on layout initialization to customize legal names, footers, invoice credentials, and logos across the user interfaces.
