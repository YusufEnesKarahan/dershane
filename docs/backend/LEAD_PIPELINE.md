# Sales Pipeline Kanban Engine

The Sales Pipeline provides a visual board tracking lead status stages:

- **Kanban Board (`/admin/crm/pipeline`)**: Renders columns matching `lead_statuses` sort orders.
- **Status Updates**: Danışman can update lead stages, triggering transition timeline entries (`lead_activities`).
- **Student Conversion**: Converting a lead to `REGISTERED` marks sales completion.
