# Blog Module

The Blog suite extends the core CMS with relational content stubs:

- **Hiyerarşik Kategoriler**: Supported via `BlogCategory` model nested children links.
- **Etiketler**: Handled via `BlogTag` model relations with tag merging logic.
- **Analytics logging**: Simple views-tracking middleware.
