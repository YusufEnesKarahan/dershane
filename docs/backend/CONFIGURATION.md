# Configuration

System-wide properties are loaded from the database:

- **SMTP Verification**: The testing utility verifies outbound socket connections before saving SMTP settings.
- **Maintenance controls**: Standard IP bypasses are parsed dynamically.
- **Storage configs**: Target storage disks can be verified via the admin panel.
